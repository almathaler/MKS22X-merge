public class Merge{
  /*sort the array from least to greatest value. This is a wrapper function*/
  public static void mergesort(int[]data){};

  private static void mergesortHelp(int[] data, int hi, int lo){
    int seperate = (hi + lo) / 2; //if range is even, index will be one closer to start than end
    //use copy of range for new array
    int[] left = copyOfRange(data, lo, seperate);
  }


}
